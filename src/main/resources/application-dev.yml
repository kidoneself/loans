# 开发环境配置（本地开发调试）
spring:
  # 数据源配置
  datasource:
    url: jdbc:mysql://43.134.58.162:3306/loans?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: loans
    password: a123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    
    # HikariCP 连接池配置
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

# 日志配置（开发环境开启详细日志）
logging:
  level:
    root: INFO
    com.finance.loans: DEBUG
    com.finance.loans.mapper: DEBUG
    org.springframework: DEBUG

